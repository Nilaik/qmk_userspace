RGB_MATRIX_ENABLE = yes

// When bootmagic is enabled, holding down the first key during bootup will enter the bootloader instead of the normal keymap. 
// This is useful for flashing new firmware without needing to open up the keyboard or use a separate reset button.
BOOTMAGIC_ENABLE = yes